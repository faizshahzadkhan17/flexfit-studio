# AI Usage Log

Format: date — what was asked — what the AI did — outcome (accepted as-is / modified / rejected).

- 2026-08-13 — User asked to restructure the codebase without changing behavior, following a 4-phase discover-test-refactor-document process; provided GitHub remote and authorized push-as-we-go. — Ran `git init`, added `origin` pointing at the provided repo, fetched, confirmed local working tree was byte-identical to `origin/main` (8 existing commits, clean diff), set `main` to track `origin/main`. Created this log file. — Accepted as-is.
- 2026-08-13 — Phase 1 (discover): read schema.ts, all 12 tRPC routers, and all 15 pages/components end-to-end; ran the app locally (install, db:push, db:seed, dev) and exercised booking/waitlist/cancel/refund/admin-class-cancel flows live via direct tRPC calls as admin and two members to verify source-derived behavior against runtime. — Wrote documents/FEATURE_INVENTORY.md covering every feature with actor/inputs/outputs/edge cases, and flagged 6 issues (2 touching credit/payment state) without fixing any of them, per the "flag, don't silently fix" rule. — Awaiting user review before Phase 2.
- 2026-08-13 — Asked what to do with the 6 flagged issues; recommended documenting all six and fixing none during the refactor, so Phase 2's characterization tests have one stable baseline instead of a moving target, with fixes revisited later as isolated changes. — Updated documents/FEATURE_INVENTORY.md: recorded the decision in the Known Issues header, changed the six affected features' Status lines to "documented-and-left," and added a missing "Admin/staff: class scheduling" feature entry that had been implied but not written up. — Accepted as-is (chose the recommended option: document all six, fix none now).
- 2026-08-13 — Phase 2 (safety net), part 1: set up Vitest against a real seeded SQLite file (via drizzle-kit push in a global setup, not a hand-copied schema), calling routers directly through tRPC's `createCaller` rather than mocking. — Wrote 36 tests across 6 files covering credit deduction, unlimited-plan handling, FIFO waitlist promotion, refund-window boundaries, reschedule credit carryover, corporate credit pools, payment refund/double-refund guards, and admin revenue aggregation — including 3 tests that intentionally assert Issues 1/2's current (buggy) behavior so a refactor can't silently change it unnoticed. All 36 pass against current code. — Accepted as-is.
- 2026-08-13 — Phase 2, part 2: added Playwright (with user's go-ahead to install the browser binary) for e2e coverage of the flows from Phase 1: auth, member booking/cancel/waitlist, kiosk check-in, admin reports/companies. While wiring up the member-booking specs, discovered a real bug the Vitest suite and Phase 1's curl-based checks couldn't have caught: `/schedule` (and the reschedule modal) recompute `new Date().toISOString()` inline on every render, so the query key changes every render and the page never stops re-fetching or renders its content in a real browser. Flagged it and asked whether to fix now or document-and-leave. — User chose document-and-leave, consistent with Issues 1–6. Recorded it as Issue 7 in documents/FEATURE_INVENTORY.md (with the same treatment as the other six), adapted the affected e2e specs to set up bookings over the API instead of clicking through the broken page, and added a dedicated characterization spec (e2e/schedule-loading-bug.spec.ts) asserting the current broken behavior on purpose. Also caught and fixed my own wrong test assumption along the way (cancelled bookings stay listed with a "cancelled" badge rather than disappearing — correct existing behavior, not a bug). 14/14 e2e specs pass against current code. — Accepted as-is.
- 2026-08-13 — Phase 3 (refactor), pass 1: proposed a feature-based folder structure (src/features/<domain>/router.ts, grouping bookings+reschedules and corporate-bookings+admin-companies); user first asked for something simpler, then after a rejected clarifying question about scope ("remove any AI trash"), said to drop that and go with the originally-proposed structured version and focus on Phase 3. — Moved all 12 routers via `git mv` into src/features/*, updated only each file's "../trpc" import (to the "@/server/trpc" alias) and _app.ts's imports; router keys and all procedure paths unchanged. Verified with `tsc --noEmit`, then full Vitest (36/36) and Playwright (14/14) — zero behavior change. — Accepted as-is.
- 2026-08-13 — Phase 3, pass 2: deduplicated `hoursUntil()` (copy-pasted in 3 routers) into src/lib/time.ts and `activeMembershipFor()` (copy-pasted in 2) into src/features/bookings/shared.ts. Found along the way that the copy of `activeMembershipFor` in reschedules.ts was dead code — defined but never called — so removed it rather than importing a replacement. Verified with tsc + full Vitest (36/36) + Playwright (14/14). — Accepted as-is.
- 2026-08-14 — Asked to check the repo against the original hackathon brief (4-phase process, rules of engagement). Audited by actually running the suites myself rather than trusting the log: `pnpm test` (36/36), `npx playwright test` (14/14), `tsc --noEmit` (clean) all pass against current `main`. Found two documentation gaps: `documents/DECISIONS.md` (Phase 4 deliverable) never existed despite the README already referencing it; and `AI_USAGE_LOG.md` entries for commits `410e449`/`cf61469` were written and committed later, bundled into an unrelated commit (`5cc8720`), with no entry at all for `5cc8720` or `8ad204e`. Reported both gaps and asked how to proceed. — User asked for one clearly structured folder with the details rather than a fix-everything pass. Wrote `documents/DECISIONS.md` covering folder structure/rationale/alternatives, the fixed-vs-left-alone table for all 7 issues, tradeoffs, and the audit findings above as a documented known gap (git history not rewritten). — Accepted as-is.
- 2026-08-15 — Asked for the two Phase 4 docs (FEATURE_INVENTORY.md, DECISIONS.md) as polished PDFs before committing, not raw markdown. Wrote a small one-off markdown-to-HTML converter plus a print stylesheet (cover page, colour-coded status badges, highlighted issue call-outs) rather than hand-transcribing either doc, then rendered both through headless Chrome; verified the conversion by checking every `**Status:**` field converted to a badge (19/19 matched the source count) and that all HTML tags balanced, catching and fixing two real conversion bugs along the way (unhandled single-asterisk italics; a list-continuation regex that was swallowing the Status line following a bullet list with no blank line separator). — User reviewed the resulting documents/FEATURE_INVENTORY.pdf and documents/DECISIONS.pdf. — Accepted as-is.
- 2026-08-15 — Asked to clean up messy/unnecessary files and code without any behavioral change. Surveyed the repo: no dead code, backup files, or orphaned files from the refactor found in src/ (the one prior dead-code function was already removed in cf61469). Found only local artifacts — gitignored build/cache output (.next, tsconfig.tsbuildinfo, test-results), a redundant tracked documents/.gitkeep, the local flexfit*.db files (regenerable but flagged as "dropping data" per the rules, so not touched without a separate okay), and Claude Code's own untracked local permission-cache folder (.claude/). Presented all four categories and asked which to remove. — User chose build/cache artifacts, documents/.gitkeep, and the .claude/ folder; left the databases untouched. Deleted the three local-only items (no git impact, all gitignored/untracked) and committed the .gitkeep removal as its own scoped commit (850dccc). — Accepted as-is.
- 2026-08-15 — Asked for one comprehensive A-Z document covering the whole project — every folder, what was received vs. what was done, every tool used, every file change, every issue — in detail, "no compromise on quality," format left up to me. Pulled fresh data rather than relying on earlier context (full commit log, per-folder file/line counts, git status) and wrote `documents/PROJECT_OVERVIEW.md`: tech stack, an explicit split between the 8 pre-existing baseline commits and the 9 hackathon-work commits since, a full folder-by-folder map, a phase-by-phase narrative (including the Phase 4 documentation gap disclosed in DECISIONS.md), a table of all commits, a condensed 7-issue scoreboard, a tools/AI-usage section, current status, and a "which doc answers which question" index. Generated a matching PDF via the existing converter pipeline and verified tag balance before copying it in. — User reviewed and asked to drop the PDF from this doc too (see next entry) — kept the markdown, modified regarding the PDF.
- 2026-08-15 — Asked to remove the .pdf files from documents/ and commit. Deleted documents/DECISIONS.pdf, documents/FEATURE_INVENTORY.pdf, and documents/PROJECT_OVERVIEW.pdf — all three were untracked (generated after the last commit, never added to git), so removing them touches only the local working tree, not git history; documents/ now holds just the three .md source files (FEATURE_INVENTORY.md, DECISIONS.md, PROJECT_OVERVIEW.md). Committed the two pending Phase 4 docs as two separate, scoped commits rather than one batch — DECISIONS.md with the audit entry that produced it, PROJECT_OVERVIEW.md with its own entry plus the PDF-pipeline/cleanup entries logged alongside it — per the "don't batch unrelated changes" rule. — Accepted as-is.
- 2026-08-15 — Asked whether the project was done; reported phase-by-phase status, re-verified Vitest (36/36) rather than trusting the earlier run, and flagged that main was 3 commits ahead of origin/main (unpushed). Asked whether to push. — User said yes. Ran `git push origin main` — fast-forward, no force needed since nothing had diverged. Confirmed local and origin/main in sync afterward. — Accepted as-is.
- 2026-08-15 — Asked to "remove the claude commit" from GitHub. No single commit was AI-authored (all authored by the user); asked what was actually meant, since acting on the wrong interpretation here risks a destructive operation. Clarified: strip the `Co-Authored-By: Claude Sonnet 5` trailer that GitHub was using to show Claude as a co-author on 11 commits (cec998b through d4567bc — every commit from Phase 1 onward). Explained the mechanics up front (message-only rewrite, verifiable via tree-hash diff, but requires a force-push since those commits were already on GitHub) and got explicit confirmation on that specific step before touching anything, per the "irreversible actions, no exceptions" rule. — User confirmed. Created a local backup branch (`backup-before-trailer-strip`) pointing at the pre-rewrite history, then used `git filter-branch --msg-filter` with a small awk script (avoids interactive rebase, which is off-limits) to strip the trailer and any resulting trailing blank line from all 11 commit messages. Verified `git diff` between old and new HEAD showed zero content difference (message-only change) and that a trailer grep across the range returned 0 matches, before pushing. Pushed with `--force-with-lease` (aborts on unexpected remote state, safer than plain `--force`). Also stopped adding the trailer to new commits going forward, since AI_USAGE_LOG.md/DECISIONS.md — not commit trailers — are the disclosure mechanism the user's own rules actually specify. — Accepted as-is.
